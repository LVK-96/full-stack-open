import React from 'react';
import { connect } from 'react-redux';

const BlogInfo = () => {
  return {
    null
  };
};

const connectedBlogInfo = connect(null)(BlogInfo)

export default connectedBlogInfo;

